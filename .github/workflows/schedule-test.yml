name: Schedule Sanity Check

on:
  schedule:
    - cron: "*/1 * * * *"   # roda a cada 1 minuto (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggered by ${{ github.event_name }} at $(date -u) UTC"
